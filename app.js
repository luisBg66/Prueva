// app.js minimal: evitar duplicar lógica que ya controla script.js
// Mantener este archivo pequeño para no redeclarar variables ni listeners.
window.addEventListener('DOMContentLoaded', () => {
    console.log('app.js cargado (módulo vacío para evitar conflictos)');
});